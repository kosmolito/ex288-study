apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: simple-username-pipeline
spec:
  params:
  - name: USERNAME
    kind: string
  tasks:
  - name: simple-username-task
    taskRef:
      apiVersion: tekton.dev/v1
      name: simple-username-task
      kind: task